# ARES default config — bundled with distribution
# These are copied to ~/.ares/ on first run, never overwritten.

model:
  backend: mlx               # mlx | llama_cpp
  base_url: http://127.0.0.1:8080/v1

agent:
  name: ARES
  mode: local                # local | cloud (local-first non-negotiable)

memory:
  path: ~/.ares/memory.db

nas:
  # Defaults — override in ~/.ares/config.yaml or ARES_NAS_ROOT env var
  root: ~/ARES_Brain         # Or any mounted volume for networked NAS
  obsidian: ~/ARES_Brain/Obsidian
  brain: ~/ARES_Brain

# Telemetry (OSC) and IPC settings live in ~/.ares/config/ares.toml.
# Example:
#
#   [telemetry.osc]
#   enabled = false
#   host = "127.0.0.1"   # render node IP
#   port = 9000
#
#   [ipc]
#   socket_path = "/tmp/ares_ipc.sock"
