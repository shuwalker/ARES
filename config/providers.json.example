{
  "schema_version": 1,
  "providers": {
    "example_runtime": {
      "enabled": false,
      "kind": "runtime",
      "endpoint": "https://provider-owned-host.example",
      "credential_env": "EXAMPLE_PROVIDER_API_KEY",
      "capabilities": [
        "conversation"
      ],
      "metadata": {}
    }
  }
}
