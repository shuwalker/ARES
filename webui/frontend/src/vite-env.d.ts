/// <reference types="vite/client" />

// Gives TypeScript the ambient module declarations for Vite's static asset
// imports (`import icon from "./icon.png"` resolves to a string URL), which
// src/assets/index.ts relies on.
