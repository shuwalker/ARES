[dependencies]
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "1.5", features = [ "shell-open", "updater", "devtools", "dialog-open", "fs-all", "path-all", "dialog-save"] }
tauri-plugin-autostart = "0.1"